# On Linux, set this to your host UID (`id -u`) so the bind-mounted ./dags and
# ./config are writable by the container user — the 50000 default only works
# unmodified on macOS/Windows, where Docker Desktop remaps file ownership.
POSTGRES_USER=deriv
POSTGRES_PASSWORD=deriv
AIRFLOW_UID=50000
