name: vendor_feed
key: [client_id, deposit_date, amount_usd]
left: "SELECT client_id, deposit_date, amount_usd FROM staging.vendor_deposits"
right: "SELECT client_id, deposit_date, amount_usd FROM staging.client_deposit"
